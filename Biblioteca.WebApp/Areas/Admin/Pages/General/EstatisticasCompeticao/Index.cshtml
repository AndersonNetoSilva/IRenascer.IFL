@page
@model IFL.WebApp.Areas.Admin.Pages.General.EstatisticasCompeticao.IndexModel

@{
    ViewData["Title"] = "Estatisticas de Competição";
}

<breadcrumb items="Cadastros, Gerais, Estatisticas Competicao"></breadcrumb>

<!-- filtro -->
<natural-filter fields="Nome"
                placeholder="Pesquisar…"
                example="Nome:João"
                value="@Model.Search">
</natural-filter>

<!-- Dados -->
<table class="table mb-3">
    <thead>
        <tr>
            <action-link-button action="IFL.WebApp.TagHelpers.ButtonActionType.Create">
            </action-link-button>
            <th class="text-center text-nowrap" style="width:1%;">
            </th>
            <th class="text-center text-nowrap" style="width:1%;">
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstatisticasCompeticao[0].Atleta.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstatisticasCompeticao[0].Evento.Nome)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.EstatisticasCompeticao)
        {
            <tr>
                <action-link-button action="IFL.WebApp.TagHelpers.ButtonActionType.Edit" route-id="@item.Id"></action-link-button>
                <action-link-button action="IFL.WebApp.TagHelpers.ButtonActionType.Details" route-id="@item.Id"></action-link-button>
                <action-link-button action="IFL.WebApp.TagHelpers.ButtonActionType.Delete" route-id="@item.Id"></action-link-button>
                <td>
                    @Html.DisplayFor(modelItem => item.Atleta.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Evento.Nome)
                </td>
            </tr>
        }
    </tbody>
</table>

<!-- Paginação -->
<pager page="/Admin/General/Pesagens/Index"
       page-number="Model.PageNumber"
       total-pages="Model.TotalPages">
</pager>